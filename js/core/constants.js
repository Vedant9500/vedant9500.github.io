/* ========================================
   CONSTANTS - Shared timing and configuration values
   ======================================== */

export const TIMING = {
    SCROLL_OFFSET: 100,             // Offset for scrollspy calculations
    MOBILE_BREAKPOINT: 768,         // Pixels threshold for mobile/desktop
    REVEAL_THRESHOLD: 0.12,         // Intersection observer threshold for reveal animations
    MOBILE_POPOVER_PADDING: 12,     // Edge padding for mobile popover positioning
};

export const sectionOrder = ['about', 'projects', 'toolbox'];
